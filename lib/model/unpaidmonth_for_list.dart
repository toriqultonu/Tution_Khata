class UnpaidMonthForList{

  var id;
  var monthId;
  String month, yearId, fee, paidAmount, waiverAmount;
  bool isSelected;

  UnpaidMonthForList(this.id, this.monthId, this.month, this.yearId, this.fee,
      this.paidAmount, this.waiverAmount, this.isSelected);
}